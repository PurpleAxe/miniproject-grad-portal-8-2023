import { appInboxDataAccess } from './app-inbox-data-access';

describe('appInboxDataAccess', () => {
  it('should work', () => {
    expect(appInboxDataAccess()).toEqual('app-inbox-data-access');
  });
});
