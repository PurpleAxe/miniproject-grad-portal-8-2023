import { apiFeedDataAccess } from './api-feed-data-access';

describe('apiFeedDataAccess', () => {
  it('should work', () => {
    expect(apiFeedDataAccess()).toEqual('api-feed-data-access');
  });
});
